[project]
name = "micrograd"
version = "0.1.0"
description = "Lennert's implementation of Micrograd"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "graphviz>=0.21",
    "matplotlib>=3.10.9",
    "numpy>=2.4.6",
    "torch>=2.12.1",
    "torchvision>=0.27.1",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "ruff>=0.15.15",
]
